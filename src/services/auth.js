const authService = async () => {
    try {
        /**
         * TODO: access database to validate login
         */
    } catch (ex) {
        console.error(ex);

    }
}

export default authService;